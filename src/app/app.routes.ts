import { Routes } from '@angular/router';
import { AnalogClock } from './analog-clock/analog-clock';
import { ImportantNotes } from './important-notes/important-notes';

export const routes: Routes = [
    { path: '', component: ImportantNotes }
];
